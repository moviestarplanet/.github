name: Release Drafter

on:
  push: [ $default-branch ]

permissions:
  contents: write
  pull-requests: read

jobs:
  release-drafter:
    uses: moviestarplanet/github-reusable-workflows/.github/workflows/release-drafter.yaml@main
